# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

version: "3.3"
services:
  app:
    build:
      context: ./containers
      dockerfile: Dockerfile.app
      args:
        BUN_VERSION: 1.0.25-alpine
    environment:
      TEST_VAR: this is the test var value
    volumes:
      - type: bind
        source: ./
        target: /app
